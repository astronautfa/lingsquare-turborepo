import { Metadata } from 'next'

export const metadata: Metadata = {
  title: "Explore | LingSquare",
  description: "LingSquare explore page",
}

export default function Example() {
  return (
    <div>
      something
    </div>
  )
}